{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug bk uninstall",
            "type": "lldb",
            "request": "launch",
            "program": "${workspaceFolder}/target/debug/bk",
            "args": ["--uninstall"],
            "cwd": "${workspaceFolder}"
        }
    ]
}
